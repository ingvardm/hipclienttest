Running the app
===

```bash
# install dependencies
$ yarn

# install pods
$ cd ios
$ pod install

# start metro bundler
$ yarn start

# run on ios simulator
$ yarn ios

# run on android device/emulator
$ yarn android
```